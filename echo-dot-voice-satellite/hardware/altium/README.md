# Altium Project

Wiring diagram / schematic for the mic array → ESP32 → MAX98357A → speaker
signal path.

Place the Altium project files (`.PrjPcb`, `.SchDoc`, exported PDFs/images)
in this directory. Export a PNG/PDF of the schematic alongside the native
files so it renders directly on GitHub without requiring Altium to view.
